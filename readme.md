# inkscape-flatten

## Development Setup

From the root of the repository checkout:

    $ python3 -m venv venv
    $ . venv/bin/activate
    $ pip install -e .

Then you can run e.g. `inkscape-flatten -h`.
